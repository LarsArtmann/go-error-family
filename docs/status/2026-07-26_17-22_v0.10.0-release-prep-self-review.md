# v0.10.0 Release Preparation — Self-Review

**Date:** 2026-07-26 17:22
**Session goal:** Prepare and verify the v0.10.0 release (Orchestration family)
**HEAD:** `4546000` (docs(docs): update project documentation and changelog)

---

## a) FULLY DONE

### Module version bumps (all 7 modules)

- Root `go.mod` → implicit v0.10.0 (root module has no self-reference)
- `diagnose/go.mod` → root v0.10.0
- `agent/go.mod` → root v0.10.0 + diagnose v0.2.2
- `bridge/go.mod` → root v0.10.0
- `diagnose/git/go.mod` → root v0.10.0 + diagnose v0.2.2
- `diagnose/postgres/go.mod` → root v0.10.0 + diagnose v0.2.2
- `examples/go.mod` → root v0.10.0 + bridge v0.3.2 + diagnose v0.2.2

### Release blocker fixed

- `examples/go.mod` had `bridge v0.0.0-00010101000000-000000000000` + local `replace` directive. Replaced with real `bridge v0.3.2`. Same bug class as the v0.6.0 hotfix. No replace directives remain anywhere.

### go.sum files updated

- All 7 modules ran `go mod tidy` with `GOSUMDB=off` to populate new version hashes
- `go.work.sum` updated with root v0.10.0 + diagnose v0.2.2 + bridge v0.3.2 hashes

### CHANGELOG.md finalized

- `[Unreleased]` → `[0.10.0] - 2026-07-26`
- Merged duplicate `### Changed` sections into one
- Added `### Modules` section with all 7 module versions
- Added `examples/go.mod` phantom replace fix to `### Fixed`

### Documentation updated

- AGENTS.md: API Surface v0.9.0 → v0.10.0, severity order updated, Orchestration added to Surprising Behaviors
- README.md: Orchestration row added to family table
- FEATURES.md: "Last verified" updated from `[Unreleased]` to `v0.10.0`
- Website changelog.mdx: v0.10.0 entry added

### Tags created (locally, NOT pushed)

All 7 tags created at `4546000`:

```
v0.10.0  diagnose/v0.2.2  agent/v0.2.2  bridge/v0.3.2
diagnose/git/v0.5.2  diagnose/postgres/v0.5.2  examples/v0.3.0
```

### Verification passed

- **Tests:** 9 packages across 7 modules, all pass with `-race -count=1`
- **Lint:** 0 issues across all 7 modules (golangci-lint)
- **Build:** All 7 modules build; `GOWORK=off go build ./...` passes for root
- **No replace directives** anywhere in the workspace
- **No phantom versions** (`00010101`) anywhere

---

## b) PARTIALLY DONE

### go.work.sum

- Updated with new hashes but **uncommitted** (`git status` shows `M go.work.sum`). The auto-git daemon hasn't picked it up yet. The tags point at `4546000` which does NOT include this change. If pushed as-is, the tagged commit's go.work.sum is slightly stale (missing root v0.10.0 hash line). Functionally irrelevant — go.work.sum is workspace-only and self-heals on next `go mod tidy`.

### ROADMAP.md

- Still references "v0.9.0 is released; `[Unreleased]` adds the `Orchestration` family." Should say v0.10.0 is released. Not updated.

---

## c) NOT STARTED

### SKILL.md family table

- Missing Orchestration row. The table at `SKILL.md` has 5 families (Rejection/Conflict/Transient/Corruption/Infrastructure) but no Orchestration entry. This is the primary API reference document for the library.

### Website API reference

- `website/src/content/docs/api-reference.mdx` has only 2 mentions of Orchestration but may lack the full family table row, constructor list, or severity ordering explanation. Not verified.

### Website build verification

- Did not run `astro check` or `astro build` to confirm the website still builds after changelog.mdx edit. The edit was small (text only) but unverified.

### Fuzz tests

- Did not run `go test -fuzz=FuzzParseFamily -fuzztime=10s ./...` or any fuzz targets. The release workflow does not run fuzz tests either, but it's good practice before a release.

### Benchmarks

- Did not run `go test -bench=. -benchmem ./...` to verify no performance regression from the Orchestration addition (severity table change, new family constant).

### TODO_LIST.md

- Not checked for v0.10.0 release items. May contain stale "ship Orchestration" tasks that should be marked done.

### Nix flake check

- Did not run `nix flake check` or `nix build`. The release workflow doesn't use Nix, but the project's AGENTS.md says to check flake.nix first.

---

## d) TOTALLY FUCKED UP

### VCS cache hack

- To work around Go's workspace module resolution failing for unpublished versions, I **manually created tags inside Go's module cache** (`/home/lars/go/pkg/mod/cache/vcs/...`). This is a dirty hack:
  - It writes to Go's internal cache directory, which is not version-controlled
  - It could confuse future `go mod` operations if the cache is in an inconsistent state
  - It was necessary because `go mod tidy` with `GOWORK=off` tries to fetch from the proxy, and the versions don't exist yet
  - The proper fix is `GOSUMDB=off GOFLAGS=-mod=mod go mod tidy`, which I eventually found — but I had already polluted the VCS cache
  - **The VCS cache tags should be cleaned up** after the real tags are pushed

### Stale gopls diagnostics ignored

- gopls reported 12 bridge errors throughout the session (`classified.Domain undefined`, `c.Code undefined`, etc.). I dismissed them as "stale module cache" without investigating. They were indeed stale (tests passed), but I should have restarted the LSP or verified more carefully rather than just ignoring 12 errors.

### Didn't catch SKILL.md gap during the session

- I updated README, FEATURES, AGENTS, CHANGELOG, website changelog — but **missed SKILL.md**, the document the project itself calls "Full API reference." I only found it during this self-review. This is the most-used doc for consumers.

---

## e) WHAT WE SHOULD IMPROVE

1. **Release checklist document** — This session was ad-hoc. A `docs/release-checklist.md` with the exact steps (bump versions → tidy → test → lint → update docs → tag → push) would prevent skipping SKILL.md or forgetting go.work.sum.

2. **Pre-release doc grep** — Before tagging, run `grep -rn 'v0.9.0\|Unreleased\|five famil' *.md docs/ website/` to catch stale references. I did this belatedly in self-review, not before tagging.

3. **Go workspace unpublished-version workflow** — The VCS cache hack was avoidable. The reliable pattern is `GOWORK=off GOSUMDB=off GOFLAGS=-mod=mod go mod tidy` in each submodule. Document this in AGENTS.md for future releases.

4. **Fuzz + bench before release** — A release that changes the severity table (core classification logic) should run fuzz tests and benchmarks. The CI workflow doesn't include these. Consider adding a pre-release manual step.

5. **Single-commit release** — The release changes are spread across multiple auto-git daemon commits (`821b0d7`, `6e1de13`, `4546000`). A clean release would squash these into one `release: v0.10.0` commit before tagging.

---

## f) Up to 50 things to get done next

### Release-blocking (before pushing tags)

1. **Add Orchestration row to SKILL.md family table** — missing 6th family
2. **Update ROADMAP.md** — "v0.9.0 is released; [Unreleased] adds..." → "v0.10.0 is released"
3. **Commit go.work.sum** — currently uncommitted, tags point at commit without it
4. **Verify website builds** — `astro check && astro build` after changelog.mdx edit
5. **Clean VCS cache hack** — remove manually-created tags from Go module cache after real tags pushed
6. **Check TODO_LIST.md** for stale Orchestration/release items to mark done

### Should-do before pushing tags

7. **Run fuzz tests** — `go test -fuzz=FuzzParseFamily -fuzztime=10s ./...` (severity table changed)
8. **Run benchmarks** — verify no regression from familyData table change
9. **Verify website API reference** — check api-reference.mdx has Orchestration constructors + family data
10. **Squash release commits** — clean history before tagging (if git-town supports it)
11. **Run `nix flake check`** — verify Nix build still works

### Post-release

12. **Push tags** — `git push origin v0.10.0 diagnose/v0.2.2 agent/v0.2.2 bridge/v0.3.2 diagnose/git/v0.5.2 diagnose/postgres/v0.5.2 examples/v0.3.0`
13. **Monitor release workflow** — verify CI passes on the tag push
14. **Verify `go get github.com/larsartmann/go-error-family@v0.10.0`** works from an external module
15. **Deploy website** — changelog page updated, needs deploy to reflect v0.10.0
16. **Update ROADMAP.md post-release** — mark Orchestration as shipped, set next focus

### Quality improvements

17. **Create `docs/release-checklist.md`** — codify the release process
18. **Add `GOSUMDB=off` workspace tip to AGENTS.md** — document the unpublished-version workflow
19. **Add fuzz step to release workflow** — at least a short fuzz run before creating the GitHub release
20. **Consider squashing auto-git commits** — investigate git-town sync to keep release history clean
21. **Restart gopls between module edits** — avoid stale diagnostics masking real issues
22. **Add Orchestration to website guides** — `guides/error-types.mdx` and `guides/classification.mdx` may need updates
23. **Update website `hero-code.ts`** — if the landing page code sample shows families, add Orchestration
24. **Review `docs/DOMAIN_LANGUAGE.md`** — add Orchestration as a domain term
25. **Consider a v0.10.0 blog post / announcement** — new family is a notable feature

### Technical debt noticed

26. **`diagnose/go.mod` and `diagnose/git/go.mod` use `go 1.26.4`** while root/agent/bridge/examples use `go 1.26.5` — inconsistent Go version across modules
27. **CI workflow tests examples** but release workflow does not — release workflow has no examples test/lint step
28. **Release workflow only lints git + postgres submodules** — agent, bridge, diagnose, examples lint steps are missing from release.yml (present in ci.yml)
29. **No `go vet` step** in either CI or release workflow
30. **`metadata.yaml` has `importance: 50`** — unclear if this needs updating for releases

### Documentation gaps

31. **SKILL.md** — full Orchestration audit needed (constructors, severity, exit code, HTTP status)
32. **Website `guides/classification.mdx`** — may need Orchestration examples
33. **Website `guides/error-types.mdx`** — may need Orchestration family entry
34. **`docs/resolving-top-5-stupidest-things.md`** — may reference "5 families"
35. **CONTRIBUTING.md** — may need release process documentation

### Future features

36. **More diagnostic rules** — Orchestration family has no diagnostic rules yet
37. **Stdlib taxonomy** — `RegisterStdlibDefaults` doesn't map any stdlib errors to Orchestration
38. **Built-in templates** — Orchestration may need default MessageTemplate entries
39. **Bridge `InferFamily`** — no domain mapping routes to Orchestration yet
40. **Orchestration-specific examples** — no `cmd/` example demonstrating Orchestration errors

### Process

41. **Tag signing** — tags are unsigned; consider signed tags for supply chain
42. **GitHub Release notes** — `generate_release_notes: true` will auto-generate from commits; may want curated notes
43. **Module proxy verification** — after push, verify `proxy.golang.org` has the new versions
44. **GoRef checker** — run `gorefs` or similar to verify all module tags are fetchable
45. **Consumer simulation** — the CI has a consumer-simulation job; verify it would pass with v0.10.0
46. **Coverage check** — Orchestration tests are new; verify root coverage didn't drop
47. **`example_test.go`** — add an `ExampleNewOrchestration` if not present
48. **Error formatting** — verify `%+v` verbose output includes Orchestration correctly
49. **JSON output** — verify `Error.JSON()` serializes Orchestration family string correctly
50. **Cross-module integration test** — verify an Orchestration error flows through bridge/diagnose correctly

---

## g) Questions I CANNOT figure out myself

1. **Should I push the tags now, or do you want to review/squash the release commits first?** The tags point at `4546000` which includes the go.mod bumps and doc updates but NOT the uncommitted go.work.sum change. Pushing as-is is functional but slightly messy.

2. **Should the release workflow (release.yml) be updated to test/lint ALL submodules like ci.yml does?** Currently it only tests git + postgres submodules and only lints git + postgres. The other 4 submodules (diagnose, agent, bridge, examples) are not verified by the release workflow, only by ci.yml. This seems like a gap but may be intentional.

3. **Do you want a clean single release commit (squash the ~4 auto-git daemon commits into one `release: v0.10.0`), or is the current multi-commit history fine?** Git-town may support this, but I didn't want to rewrite history without your call.
